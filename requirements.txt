# Pinned to the versions verified in the build environment (Python 3.11; 3.11+ required, tested layout for 3.13).
# CPU-only by default. For an NVIDIA GPU install `fastembed-gpu` instead of `fastembed` and set
# BORGES_EMBED_PROVIDERS=CUDAExecutionProvider (optional; CPU is fast enough for a personal library).
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
starlette==1.6.0
httpx==0.28.1
mcp==1.30.0
numpy==2.4.6
fastembed==0.8.1
onnxruntime==1.30.0
tokenizers==0.23.2
pymupdf==1.28.2
python-docx==1.2.0
EbookLib==0.20
beautifulsoup4==4.15.0
lxml==6.1.3
watchdog==6.0.0
pytest==9.1.1
